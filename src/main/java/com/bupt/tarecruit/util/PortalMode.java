package com.bupt.tarecruit.util;

/**
 * Distinguishes which end-user portal an application instance serves.
 */
public enum PortalMode {

    /**
     * Teaching assistant portal (registration and dashboard for TAs).
     */
    TA_PORTAL,

    /**
     * Module organizer portal (registration and dashboard for MOs).
     */
    MO_PORTAL
}
